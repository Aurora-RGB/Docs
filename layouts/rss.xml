{{- $authorName := "Aurora-RGB Community" }}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = .Site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
{{- $pages = $pctx.RegularPages }}
{{- else }}
{{- $pages = $pctx.Pages }}
{{- end }}
{{- $limit := .Site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
{{- $pages = $pages | first $limit }}
{{- end }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Aurora-RGB Documentation</title>
        <link>{{ .Permalink }}</link>
        <description>Aurora-RGB Documentation pages</description>
        <generator>Hugo</generator>
        <language>{{ site.Language.LanguageCode }}</language>
        <managingEditor>{{ with $authorName }} {{ . }}{{ end }}</managingEditor>
        <webMaster>{{ with $authorName }} ({{ . }}){{ end }}</webMaster>
        <copyright>{{ with .Site.Copyright }}{{ . }}{{ end }}</copyright>
        {{ if not .Date.IsZero }}
        <lastBuildDate>{{ (index $pages.ByLastmod.Reverse 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
        {{- with .OutputFormats.Get "RSS" }}
        {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
        {{- end }}
        {{- range $pages }}
        <item>
            <title>{{ .Title }}</title>
            <link>{{ .Permalink }}</link>
            <pubDate>{{ .Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
            <author>{{ with $authorName }}{{ . }}{{ end }}</author>
            <guid>{{ .Permalink }}</guid>
            <description>{{ .Params.description | transform.XMLEscape | safeHTML }}</description>
        </item>
        {{- end }}
    </channel>
</rss>